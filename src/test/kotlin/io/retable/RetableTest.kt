package io.retable

import org.junit.jupiter.api.Test
import strikt.api.expect
import strikt.assertions.isEqualTo

class RetableTest {
    @Test
    fun `should have a first test`() {
        expect(true).isEqualTo(true)
    }
}